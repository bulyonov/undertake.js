**Undertake.js** is a simple contract programming library. Adds functionality to check params and results of functions

`var sum = function (a,b) {
     return a + b;
 }
 .expects('integer', 'float')
 .returns('float');`

 *More examples will be added later*